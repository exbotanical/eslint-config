key: value
another_key: [item1,  item2,  item3]
badly-formatted-list:
  - item one
  - item two

  - item three

nested:
  key: value with extra spaces
  another_key: value
  misaligned:
    -item1
    - item2

# Missing space after colon, inconsistent indentation, and incorrect list formatting
array:
  -one
  -two
  - three

object: {nested_key :"value", another_key:"value"}

# Inconsistent boolean formatting
enabled: Yes
debug: no
flag: True

# Misformatted numbers
some_number: 123
float_value: .5

# Trailing whitespace
trailing: 'This has extra spaces at the end.    '

x:
  - {a: b,c: d}


# BAD
BAD:

r: 2
